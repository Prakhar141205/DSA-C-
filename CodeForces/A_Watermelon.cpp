#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;   // number of test cases
    
    
    if( t > 2 && t % 2 == 0) cout << "YES\n" ;
    else cout << "NO\n";
    

    return 0;
}